Hola, el proyecto se creo a base de otro proyecto que tenia guardado, se utilizo para la APP el sdk 29 y retrofit para conectarse al back, del lado del backend se utilizo springboot un rest sencillo pero eficaz, y en base de datos se utilizo MySql por facilidad de ambientar con XAMPP pero facilmente se podria cambiar a Postgrest solo se cambia el archivo application.properties pero primero se crea la base de datos con base al archivo ambiente.sql para poder correr la apk en un dispositivo movil fisico se requiere conectarse en la misma red pero se tiene que compilar de nuevo ya que la ip del backend cambia a base la ip local del equipo donde se ejecuta el .jar para probar el proyecto se requiere correr el comando java -jar archivo.jar application.properties  en la consola de windows, y una vez echo esto se requiere cambiar la ip en el proyecto android para que apunte a la ip local del equipo donde se ejecuto el comando, la pantalla principal de la app es un listado con lo usuarios, contiene un boton en la esquina inferior para agregar un nuevo prospecto y para modificar su estatus, se puede ingresar dando click en un prospecto de la lista 