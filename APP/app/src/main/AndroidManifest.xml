<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.vp.springclient">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.SpringClient"
    android:usesCleartextTraffic="true">
    <activity
      android:exported="true"
      android:label="Listado de Prospectos"
      android:name="com.vp.springclient.EmployeeListActivity" >
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity
      android:exported="true"
      android:name="com.vp.springclient.EmployeeForm"
      android:label="Registrar Prospecto"
      android:parentActivityName="com.vp.springclient.EmployeeListActivity">
    </activity>
    <activity
        android:exported="true"
        android:name="com.vp.springclient.EmployeeDetailsActivity"
        android:label=""
        android:parentActivityName="com.vp.springclient.EmployeeListActivity">
    </activity>
  </application>
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

  <uses-permission android:name="android.permission.INTERNET" />

</manifest>